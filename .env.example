# Postgres connection string. In Dokploy, point this at the attached database
# service; locally, docker compose up provides the value below.
DATABASE_URL=postgres://mytraining:mytraining@localhost:5432/mytraining

# Timezone used for "today" when rolling over the training day.
TZ=Atlantic/Canary
